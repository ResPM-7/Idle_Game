# 데이터 관리 및 저장 (KImJiWoongTest)

## 편집 위치

- `Assets/03.Data/Units/Player.asset`: 아군 9종을 한 파일 안의 서브에셋으로 관리.
- `Assets/03.Data/Units/Enemy.asset`: 적/보스 2종을 한 파일 안의 서브에셋으로 관리.
- `Assets/Resources/GameData/UnitDatabase.asset`: 두 분류를 합쳐 검색하는 카탈로그.
- 위 에셋을 선택하면 ID/이름 검색과 유닛 상세 편집이 인스펙터에 표시된다.
- 기존 `UnitDataSO` 형식 및 진화 참조를 유지한다. partial 클래스나 추가 필드 스크립트는 없다.
- 아군과 적은 같은 ID를 사용할 수 있으므로 딕셔너리가 분리되어 있다.
  `database.Find(201)`은 아군, `database.Find(201, true)`는 적이다.
- 새 항목은 기존 구글 시트 동기화를 실행하면 해당 통합 파일에 추가된다.
  시트 열 형식은 기존 그대로이며 스키마 자동 생성/양방향 업로드 기능은 포함하지 않는다.
- 길드/소환 비용은 UnitSpawner, 판매 가격은 UnitSellDropZone,
  훈련소 설정은 BarracksSystem에서 계속 편집한다.

## 저장/불러오기

GameSaveService는 게임 씬의 오브젝트에 미리 컴포넌트로 추가한다. 자동 생성하지 않는다.
씬 이동 시 유지하려면 인스펙터의 Is Dont Destroy를 체크한다.
MainScene 초기화가 끝나면 저장이 있을 때 자동 복원한다.
10초 간격, 기존 타이틀 이동 버튼, 종료, 앱 일시정지 시 저장한다.
드래그 중에는 잘못된 슬롯 저장을 방지하기 위해 저장을 건너뛴다.

저장 항목: 골드, 크레딧, 길드 등급, 현재 스테이지, 훈련소 강화 레벨,
그리드 슬롯별 유닛 ID, 파티 슬롯별 유닛 ID.
체력/투사체/적 위치/현재 웨이브/스킬 쿨타임은 저장하지 않는다.
복원 후 해당 스테이지 첫 웨이브에서 시작 대기한다.
SO 기본 능력치는 저장 파일에 복사하지 않고 현재 데이터에서 읽는다.

파일 위치: `Application.persistentDataPath/guild-save-v1.json`.
쓰기 완료된 임시 파일로 교체하고 직전 저장은 `.bak`으로 유지한다.
파싱/버전/슬롯/유닛 ID 오류가 나면 자동 저장을 중단해 원본을 보존한다.
손상된 저장은 게임을 종료하고 원본을 따로 보관한 다음 `.bak`을 복사해 복구할 수 있다.
로컬 JSON이므로 사용자가 직접 편집할 수 있으며 서버 보안 저장은 아니다.

플레이 중 `Tools > Game Data > Save Play Progress / Load Play Progress`로 수동 테스트.
`Open Save Folder`로 저장 폴더 확인.
강화 레벨은 현재 설정의 증가량/배율로 재계산하므로 밸런스 설정 변경이 복원에도 반영된다.

## 변환과 백업

기존 개별 유닛 파일은 통합 파일로 옮기고 씬/프리팹/진화 참조를 교체했다.
원본과 수정 전 참조 파일은 프로젝트 루트 `DataMigrationBackup/날짜시간`에 보관한다.
백업 폴더는 Git에서 제외되어 있다. 브랜치의 기존 커밋으로도 변경을 비교할 수 있다.
단순히 이전 유닛 파일만 다시 넣으면 중복 ID가 생기므로 백업 복구 시 참조 파일도 함께 복구해야 한다.
`Rebuild Unit Database`는 현재 목록을 다시 모으고 중복 ID를 검사한다.

## 검증

Unity 6000.3.17f1 배치 컴파일 및 MainScene 플레이 모드에서
`GameDataSmokeTest.Run -guildSaveSmokeTest` 실행.
테스트 세이브는 실제 사용자 파일과 분리된다.
아군/적 동일 ID 구분, 진화 참조, 재화/등급/강화/슬롯 저장 왕복 및 반복 복원 등을 확인한다.
테스트 브랜치 변경은 자동 커밋/푸시하지 않는다.
